冒泡排序：
1.基本思想
    给定一个数组，把数组中的元素统统倒入水池中，这些元素通过相互比较，按照大小顺序一个一个地像气泡一样从大到小冒出水面。


2.空间复杂度
    借助一个临时变量来存储中间值，故空间复杂度为O(1)。


3.时间复杂度
    最优情况需要进行 n-1 次比较，两两交换次数为0，时间复杂度为O(n)；
    给定的是逆序数组，则需要n(n-1)/2次比较，时间复杂度为O(n^2)；
    故杂乱无序的数组平均时间复杂度为O(n^2)。

4.稳定的排序算法。
